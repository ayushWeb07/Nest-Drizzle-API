services:
  postgres:
    image: postgres
    container_name: nest-drizzle-postgres
    restart: always
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root123
      POSTGRES_DB: nest-drizzle-db